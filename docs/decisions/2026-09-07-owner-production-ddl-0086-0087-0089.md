# Owner 授權：正式庫 DDL（0086 / 0087 / #259 加欄位）＋ 指向 TEST 的 Preview

- 日期：2026-09-07
- 相關 Issue：#50、#8（#8-B）、#259、#27、#31
- 授權形式：Owner 於本輪對三個具名問題各自作答（逐次具名，非概括授權）

---

## 一、正式庫 DDL：授權套用 `0086` 與 `0087`

**Owner 選項原文**：「兩支都授權（0086 + 0087）」

### 授權範圍

| migration | 來源 | 內容 | 影響面 |
|---|---|---|---|
| `0086_keyword_reply_images_bucket.sql` | 已在 `main`（PR #264 / squash `83ab9f0`） | 一句 `insert into storage.buckets … on conflict do nothing` | 只新增一個 public 圖片 bucket；**不改任何既有物件、不改任何政策** |
| `0087_issue_8b_tour_orders.sql` | PR #271（合併後才套用） | 新增 `tour_orders` 表、三個列舉、四支函式、三個索引、RLS 政策與 REST DML ACL | **全部只新增**；不 `alter`、不 `drop` 任何既有表／欄位／政策；可重入 |

**這是逐次具名授權，只涵蓋這兩支 migration、只涵蓋正式庫
`egehnijjpgijmccagxac`，不構成後續 Production DDL／DML／部署的概括授權。**

### 為什麼需要

兩者都是同一個形狀：**程式碼已上線，schema 沒跟上**——而且兩次都是本輪合併造成的。

- `0086`：PR #264 合併後，`/tenant/keyword-replies` 的「附加圖片」input 不再
  `disabled`，但正式庫沒有那個 bucket，店家選圖會 `500 SYS_001`，畫面顯示
  「圖片上傳失敗」。**比合併前那句誠實的「尚未建置」更糟。**
- `0087`：`/tenant/tour-orders` 的四個寫入動作與六支路由在 #271 之後會上線，
  但正式庫沒有 `tour_orders` 表，整條鏈路仍不通。

### 執行紀錄：`0086`（2026-09-07 已完成）

**套用前**（唯讀，`select id, public from storage.buckets order by id`）：

```
bug-report-attachments (false)   chat-images (true)      portfolio-images (true)
product-images (true)            richmenu-assets (true)  service-images (true)
staff-avatars (true)             welcome-card-images (true)
→ 共 8 個，沒有 keyword-reply-images
```

**套用**：`apply_migration(project=egehnijjpgijmccagxac, name=0086_keyword_reply_images_bucket)`，
SQL 逐字取自 `main` 上的檔案（避免 #197 的 branch-only 身分漂移；
migration name 用 main 的最終檔名，PB-017）。

**套用後**（唯讀）：

```
id=keyword-reply-images  name=keyword-reply-images  public=true
created_at=2026-09-07 23:17:38.435662+00
```

**行為驗證**（唯讀，不上傳任何物件）——用「bucket 不存在」與「物件不存在」
兩種錯誤碼的差異作為證據：

```
GET …/storage/v1/object/public/keyword-reply-images/does-not-exist.jpg
  → {"error":"not_found","message":"Object not found","code":"NoSuchKey"}   ← bucket 存在

GET …/storage/v1/object/public/no-such-bucket-xyz/does-not-exist.jpg
  → {"error":"Bucket not found","message":"Bucket not found","code":"NoSuchBucket"}
```

`POST /api/upload`（未登入）仍回 `401`，代表 `requireTenant()` 閘門未受影響。

### 執行紀錄：`0087`

待 PR #271 合併進 `main` 後套用，並以同樣格式補記套用前後的唯讀證據。
**不在合併前套用**——source 必須來自已合併的 `main`（#197）。

---

## 二、Playwright 驗收環境：授權建立指向 TEST Supabase 的 Preview

**Owner 選項原文**：「給一個指向 TEST Supabase 的 Preview」

#27 第 19 格、#50 第 10 格、#31 第 7 格卡在**同一件事**：需要一個
「能寫入、但不是正式庫」的已部署站點。

現況：`vercel.json` 的 `deploymentEnabled` 只開 `main` 與 `preview/**`，
而本輪的分支叫 `product/**` 與 `docs/**`，**從來不會產生 Preview 部署**。

授權內容：為 Vercel 的 **Preview 環境**（不是 Production）設定指向 TEST 專案
`nmwhwngojosmagjuvxol` 的環境變數，並以 `preview/**` 分支產生部署，
在該站點跑完整的瀏覽器實測。

**邊界**：只動 Preview 環境變數，不動 Production 的任何設定；
TEST 專案本就有長期授權（`docs/AGENT-EXECUTION.md` §3.1）。

---

## 三、#259 五個欄位：授權加欄位的 migration

**Owner 選項原文**：「授權加欄位的 migration」

`trips` 表缺 `tagline` / `exclusions` / `notices` / `meeting_point_map_url` /
`refund_policy_type` 五個欄位。行程詳情頁編輯得到、`tripApiPayload()` 不會送、
資料庫也沒有欄位可存——目前以「此欄位尚未建立資料庫欄位，儲存後不會保留」
誠實標註（PR #267）。

授權內容：寫一支**只 `add column if not exists`** 的 migration，套用後
移除頁面上的誠實註記，並讓 `tripApiPayload()` 帶上這五個欄位。

**邊界**：只新增欄位，不改任何既有欄位的型別、預設值或 not-null 約束。

---

## 不在本次授權範圍

- 真實顧客通知（LINE push／Email）——#27 ②③ 的 Preview 實測只驗「不送出」的分支。
- 真實金流、退款。
- 手動 Production 部署／promote（`main` 的自動部署不受影響）。
- 其他任何 migration。
