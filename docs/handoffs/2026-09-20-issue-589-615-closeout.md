# #589／#615 closeout checkpoint

- Base: `origin/main` / `cc82a9bb498388b4c46578b1d0714cdf1689db3f`
- Product change: #615 已 squash merge；此文件 PR 僅記錄測試教訓與交接，不修改產品、workflow、測試或規則。
- Root cause: `getByText(body)` 可先命中仍保留相同文字的 React textarea，保存完成前便開始等待通知狀態；修正改為先等待「內容」field hidden，再斷言保存結果。
- Exact head: `6cd5706b05c31ce8798b79a99fd39088b6a1fdc1`
- Evidence: check CI `35490699629` 成功；isolated `35490699595` 的 integration、E2E、cleanup 成功。
- Non-evidence: #611 PARKED；G3 r15 `35488656009` E2E 失敗且 cleanup 未跑，不作 release evidence。
- Blocked action: 手動 r16 workflow 被平台 automatic approval 拒絕，理由是未明確授權 shared TEST mutation／production-gated advancement；未重試或 dispatch 該手動 workflow，未操作 DB。#615 merge 自動觸發的 main CI `35515282818` 另在執行中，不把它寫成手動 G3 證據。
- Remaining chain: 平台解鎖後重新取 current main／時間 → G3 `TEST_VERIFIED`（含 cleanup／post-TEST）→ readonly collect 沿用同 G3 releaseId → exact frozen bundle G5 → policy-controlled G6/G7 → main schema receipt → #611 `ACTIVATE` → 授權範圍內正式驗收。此輪手動 G3 未啟動。
- Next agent: 以 live GitHub 重新確認 PR head、required checks 與 branch protection；不要修改歷史 Run 數字或把失敗／未執行的 workflow 當成證據。
